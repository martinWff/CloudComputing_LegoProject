config:
  target: "http://localhost:8080"   # Your Spring Boot base URL
  phases:
    - duration: 60                  # Run for 60 seconds
      arrivalRate: 5                # 5 new users per second
  defaults:
    headers:
      Content-Type: "application/json"